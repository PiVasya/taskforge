<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
  <!-- Оставляем доступную версию FuzzySharp -->
  <PackageReference Include="FuzzySharp" Version="2.0.2" />

  <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="9.0.9" />
  <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.8.0" />
  <PackageReference Include="Microsoft.IdentityModel.Tokens" Version="8.14.0" />

  <!-- Выравниваем провайдер и инструменты EF Core на 9.0.1 -->
  <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="9.0.1" />
  <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="9.0.1">
    <PrivateAssets>all</PrivateAssets>
    <IncludeAssets>runtime; build; native; contentfiles; analyzers</IncludeAssets>
  </PackageReference>

  <PackageReference Include="Konscious.Security.Cryptography.Argon2" Version="1.3.1" />

  <!-- S3 compatible storage (MinIO) for editor images and other media -->
  <PackageReference Include="AWSSDK.S3" Version="3.7.403.2" />

  <!-- Image compare for image-test -->
  <PackageReference Include="SixLabors.ImageSharp" Version="3.1.11" />

  <!-- Image alignment + feature matching (ORB) for image-test -->
  <!-- Pin to a NuGet-published build to avoid NU1603 (older build removed from feed). -->
  <PackageReference Include="OpenCvSharp4" Version="4.11.*" />
  <!--
    OpenCV native runtime.

    IMPORTANT:
    - Sdcb.OpenCvSharp4.mini.runtime.debian.12-x64 is a legacy package (stuck on OpenCV 4.8.x on NuGet)
      and has no 4.11.* versions, which breaks `dotnet restore` (NU1102).
    - Use the generic glibc build instead; it runs fine on Debian 12 (bookworm) base images.
  -->
  <PackageReference Include="Sdcb.OpenCvSharp4.mini.runtime.linux-x64" Version="4.11.0.35" />

</ItemGroup>



</Project>
