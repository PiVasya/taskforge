# primary or standby
TASKFORGE_NODE_ROLE=primary

# Later failover scripts will use these values. They are not required for the
# first /ha/primary-ready endpoint.
HA_PRIMARY_HOST=primary.taskforge.by
HA_STANDBY_HOST=standby.taskforge.by
HA_PUBLIC_HOST=taskforge.by
