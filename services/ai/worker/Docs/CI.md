# CI

The repository now contains `.github/workflows/build-dotnet-ai-agent.yml`.

It restores, builds and tests only the new .NET AI agent folder. Python AI worker is deprecated and should stay manual/no-op.
