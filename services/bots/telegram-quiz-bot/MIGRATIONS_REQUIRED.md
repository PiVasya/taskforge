# Миграции нужны для telegram-quiz-bot, если оставляем его собственное состояние

БД: `taskforge_telegram_quiz`.

Старые миграции сохранены, новые не генерировались.

Долгосрочно лучше вынести quiz progress в `solutions-service`, а бот оставить adapter-ом без собственной доменной БД.
