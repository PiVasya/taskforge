# Telegram Quiz verbose debug logs.
# true  = print detailed Console.WriteLine traces for every message/callback/quiz selection.
# false = disable the noisy trace layer.
TELEGRAM_QUIZ_VERBOSE_CONSOLE_LOGS=true
