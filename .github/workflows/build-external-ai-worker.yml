name: Build legacy Python AI worker (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "taskforge-ai-worker-external is deprecated."
          echo "Use taskforge-ai-agent-dotnet and build-dotnet-ai-agent.yml instead."
