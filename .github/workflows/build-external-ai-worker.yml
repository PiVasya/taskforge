name: Build & Push External AI Worker

on:
  push:
    branches: [ develop ]
    paths:
      - "taskforge-ai-worker-external/**"
      - "docker-compose.external-ai-worker.yaml"
      - ".env.external-ai-worker.example"
      - ".github/workflows/build-external-ai-worker.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        uses: docker/build-push-action@v6
        with:
          context: ./taskforge-ai-worker-external
          push: true
          tags: ghcr.io/${{ env.OWNER_LC }}/taskforge-ai-worker-external:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
