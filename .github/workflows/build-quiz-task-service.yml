name: Build & Push Quiz Task Service

on:
  push:
    branches: [ develop ]
    paths:
      - "quiz-task-service/**"
      - ".github/workflows/build-quiz-task-service.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        uses: docker/build-push-action@v6
        with:
          context: ./quiz-task-service
          push: true
          tags: ghcr.io/${{ env.OWNER_LC }}/taskforge-quiz-task-service:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
