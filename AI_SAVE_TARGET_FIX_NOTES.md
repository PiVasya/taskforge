# AI save target / course context fix

Что исправлено:
- AI больше не теряет выбранный курс в свободном чате: backend учитывает заголовок чата, всю историю пользовательских сообщений и memory.activeCourseId.
- memory_patch теперь мержится, а не затирает всю память чата. lastGapAudit/currentDraftBlueprint/activeCourseId больше не пропадают после следующего сообщения.
- Генерация задач прокидывает selectedCourseId/selectedCourseTitle/placement в bundle и в каждую задачу.
- Для запросов перед матрицами/двумерными массивами worker пытается поставить placement перед первым заданием на матрицы и после предыдущего задания.
- Кнопки 'в черновик' на фронте блокируются, если AI не определил курс/позицию, чтобы задания не выглядели как сохранённые.
- Backend больше не ставит polish-run в очередь, если невозможно определить курс для скрытого черновика.
- Вопросы вида 'в какой курс сохранены' больше не роутятся как новая генерация задач.
- polish_assignment_draft больше не оставляет selectedCourseId=null, если курс пришёл из request/task/context.
- Миграции не добавлялись.
