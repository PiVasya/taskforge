# No migrations here

`database-splitter` is a future migration/cutover tool, not a database-owning runtime service.

Do not generate EF migrations here. Generate migrations only in service API projects that own a database.
